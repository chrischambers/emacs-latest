# -*- mode: snippet -*-
# name: template
# key: template
# --
#lang racket

;; implementation:
;; ----------------------------------------------------------------------

${1:$(concat "(define (" yas-text ") ...)")}

;; interface:
;; ----------------------------------------------------------------------
(provide (contract-out
          ;; ${2:documentation}
          [${1:value}]))

(module+ test
  (require rackunit rackunit/spec rackunit/text-ui)

  (run-tests
   (test-suite
    "specifications"
    (describe
     "$1"
     (it "does things"
       $3
         )))))$0
