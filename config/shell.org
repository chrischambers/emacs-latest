#+TITLE: Shell Configuration
#+AUTHOR: Chris Chambers
#+EMAIL: magma.chambers@gmail.com
#+LANGUAGE: en-gb
#+startup: align shrink inlineimages latexpreview content
#+property: header-args:elisp :results value silent

* Enable lexical binding
  #+begin_src elisp
    ;; -*- lexical-binding: t -*-
  #+end_src

* <<<vterm>>> ([[https://github.com/akermu/emacs-libvterm][github]])
  #+begin_src elisp
    (use-package vterm
      :general
      ('(insert motion) vterm-mode-map "M-p" 'vterm-send-up)
      ('(insert motion) vterm-mode-map "M-n" 'vterm-send-down)
      (leader "av" 'vterm))
  #+end_src

* Keybindings
  #+begin_src elisp
      (defun my/toggle-comint-autoscroll ()
        (interactive)
        (customize-set-variable 'comint-scroll-to-bottom-on-output
                                (not comint-scroll-to-bottom-on-output )))

      (general-def
        local-leader eshell-mode-map
        "a" '(my/toggle-comint-autorscroll :which-key "toggle autoscroll"))
  #+end_src
